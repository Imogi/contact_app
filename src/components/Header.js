// Header component

const Header = () => {
    return (
        <header>
            <h1 style={headerTextStyle}>Resonate with your Contacts!</h1>
        </header>
    )
}

// CSS style


const headerTextStyle = {
    color: 'white',
}

export default Header